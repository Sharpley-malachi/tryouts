from datetime import datetime

# --- CONFIGURATION ---
SENSITIVITY_THRESHOLD = 0.05
# Minimum number of occurrences to constitute a 'confirmed' pattern
MIN_PATTERN_CONFIRMATION = 3  

# --- CORE TYPES ---
# Re-using some core types from the broader system if available, 
# but defining minimal local types to keep AI #3 autonomous as requested.
